---
p: high
---
Add a dependency-graph view as a tab at the top of the web UI. In ui/index.html: a two-tab switcher in the header ("Board" / "Graph", plain buttons matching the chip style); Board is today's view, Graph renders every card as a small node grouped into topological layers left-to-right (cards with no `needs` in layer 0, a card sits one layer right of its deepest dep; unknown/missing dep ids still render as a ghost node). Draw edges as SVG lines/paths between node boxes. Node styling reuses board semantics: done cards tinted like the done column, blocked cards muted with the ⛓ chip, cards with an active session show their status chip (`a-*` classes). Clicking a node opens the same card panel as clicking the card on the board. No new endpoints — everything needed (`needs`, `blocked`, columns, sessions) is already in /api/board. No dependencies, no framework; keep the file's terse vanilla-JS style. Verify with a node assertion script for the layering function (cycles must not hang — cap layers at card count and render anyway) plus the existing suite via npm run test:agents.
