{
  "description": "Injects kanban board context on every prompt",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/kanban-context.sh",
            "timeout": 1000
          }
        ]
      }
    ]
  }
}
